define makePrinter(prefix:string) {
    define mprint(message) {
        print (prefix + ":" + message);
    }
    return mprint;
}

var errorPrinter = makePrinter("ERROR");
var infoPrinter = makePrinter("INFO");